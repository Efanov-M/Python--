# 📦 CLI-утилита для работы с Яндекс.Диском и API cataas.com

Учебный проект на Python, реализующий CLI-утилиту для взаимодействия с **REST API Яндекс.Диска** и **API сервиса cataas.com** (картинки котов).

Проект выполнен в рамках курсовой работы с акцентом на:

- объектно-ориентированное программирование (ООП);
- работу с REST API;
- декораторы;
- обработку ошибок;
- логирование;
- CLI-интерфейс.

---

## 🚀 Возможности

- Получение информации о дисковом пространстве Яндекс.Диска
- Просмотр списка файлов на Яндекс.Диске
- Создание папок на Яндекс.Диске
- Загрузка файлов:
  - с локального компьютера;
  - по внешнему URL (без сохранения локально);
- Получение изображений котов:
  - без текста;
  - с пользовательским текстом;
- Загрузка изображений котов напрямую на Яндекс.Диск;
- Постраничный (pager) вывод длинных списков;
- Аудит API-взаимодействий (логирование в CSV);
- Централизованная обработка ошибок.

---

## 📂 Структура проекта

.
├── main.py             # Точка входа, CLI-логика
├── yandex.py           # Работа с API Яндекс.Диска
├── cat.py              # Работа с API cataas.com
├── errors.py           # Пользовательские исключения
├── log_decoratot.py    # Декоратор аудита API-вызовов
├── loggerAPI.py        # Логирование в CSV
├── decorators.py       # CLI-декораторы (pager)
├── requirements.txt    # Зависимости
└── README.md
---

## 📌 Описание файлов

### `main.py`

Главный CLI-модуль.

Отвечает за:

- запуск программы (`run`);
- ввод OAuth-токена Яндекс.Диска;
- отображение меню;
- обработку пользовательского ввода;
- вызов бизнес-логики приложения.

---

### `yandex.py`

Класс `Ya_Disk` — клиент REST API Яндекс.Диска.

Реализует:

- GET / PUT / POST запросы;
- загрузку файлов с локального компьютера;
- загрузку файлов по внешнему URL;
- создание папок;
- получение информации о диске;
- получение списка файлов.

Все сетевые методы:

- используют единый слой проверки HTTP-ответов;
- выбрасывают `YaDiskAPIError` при ошибках;
- оборачиваются декоратором аудита.

---

### `cat.py`

Класс `CatImage` — клиент API `cataas.com`.

Возможности:

- получение случайного изображения кота;
- получение изображения с текстом;
- хранение метаданных изображения (`id`, `url`, `mimetype`);
- генерация URL для последующей загрузки на Яндекс.Диск.

Ошибки API обрабатываются через `CatApiError`.

---

### `errors.py`

Централизованные пользовательские исключения:

- `YaDiskAPIError`
- `CatApiError`

Каждое исключение:

- хранит HTTP-код ответа;
- хранит сообщение API;
- используется как контракт между слоями приложения.

---

### `log_decoratot.py`

Декоратор `@audit()` — аудит API-взаимодействий.

Фиксирует:

- дату и время вызова;
- имя класса;
- имя метода;
- статус выполнения (`SUCCESS / ERROR`);
- HTTP-код ошибки;
- сообщение ошибки.

Особенности:

- не пишет в файл напрямую;
- не изменяет поведение оборачиваемых методов;
- не подавляет исключения;
- передаёт данные в `LoggerApi`.

---

### `loggerAPI.py`

Класс `LoggerApi` — логирование в CSV-файл.

Реализует:

- создание CSV-файла лога;
- запись заголовков;
- добавление строк аудита;
- чтение содержимого лога.

Формат записи:
datetime, class, method, status, http_code, message

---

### `decorators.py`

CLI-декораторы.

Содержит:

- декоратор `paged_output` для постраничного вывода длинного текста
  с использованием `less` (или fallback-режима).

---

### `requirements.txt`

Список зависимостей проекта.

---

## 🔐 Безопасность

- OAuth-токен Яндекс.Диска **не хранится в коде**;
- Токен вводится пользователем при запуске программы;
- В репозиторий не добавляются секреты;
- Проверка утечек выполняется через `.gitignore` и ручную ревизию.

---

## 📝 Примечание

Проект является учебным.
Некоторые решения сделаны с целью закрепления тем ООП, декораторов и архитектуры,
а не ради минимализма реализации.

---
